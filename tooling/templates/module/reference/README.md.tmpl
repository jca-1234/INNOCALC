# Reference Evidence

Record source document, revision, author, page, extraction procedure and permitted
use for every worked example. Keep originals unchanged. Record tolerances and
expected intermediate values in tests; generated reports belong under artifacts/.
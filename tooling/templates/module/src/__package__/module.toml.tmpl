id = "$module_id"
name = $name_json
short = $name_json
standard = $standard_json
folder = $folder_json
category = $category_json
maintainer = $owner_json
status = "planned"
entry = "$package.headless"
defaultSubject = $name_json
calcType = $name_json
description = "Not implemented or verified. Not available for design use."
contractVersion = 1
inputSchemaVersion = 1
capabilities = ["compute", "render", "validate"]